{
  "name": "@naaval/carrier-enterprise-ios",
  "private": true,
  "version": "0.1.0",
  "description": "Enterprise iOS packaging workspace for Naaval Carrier App.",
  "type": "module",
  "scripts": {
    "build:web": "node ./scripts/prepare-web.mjs",
    "native:sync": "npm run build:web && npx cap sync ios",
    "native:add:ios": "npm run build:web && npx cap add ios",
    "native:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "typescript": "^6.0.3"
  }
}
